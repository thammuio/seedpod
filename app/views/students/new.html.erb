<div class="row">
  <div class="col-lg-12">
    <h4 class="page-header">
      <small><%= @student_course.course_id %> - <%= @student_course.course_name %> <i class="fa fa-chevron-right"></i></small> Add Students
      <small><%= link_to "Student list", student_course_students_path(@student_course), :class => "pull-right btn btn-outline btn-primary btn-xs" %></small>
    </h4>
  </div>
</div>
<%= render 'form' %>